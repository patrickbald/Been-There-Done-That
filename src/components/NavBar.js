import React, { Component } from 'react';
import Header from '../../../React/udemy-class/final_course_projects/contactmanager/src/components/layout/Header';

const NavBar = props => {
    return(
        <nav className="navbar">
            
        </nav>
    );
}

export default Header;